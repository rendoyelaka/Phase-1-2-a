/**
 * overlay_detect.h — Phase 5 Step 102
 */
#ifndef OVERLAY_DETECT_H
#define OVERLAY_DETECT_H

/**
 * check_overlay()
 * Returns 1 if overlay/tapjacking tool detected, 0 if clean.
 */
int check_overlay(void);

#endif /* OVERLAY_DETECT_H */
