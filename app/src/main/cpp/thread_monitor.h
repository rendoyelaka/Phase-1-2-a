/**
 * thread_monitor.h — Phase 5 Step 104
 */
#ifndef THREAD_MONITOR_H
#define THREAD_MONITOR_H

/**
 * check_thread_monitor()
 * Returns 1 if injected thread or timing anomaly detected, 0 if clean.
 */
int check_thread_monitor(void);

#endif /* THREAD_MONITOR_H */
